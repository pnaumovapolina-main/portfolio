version: '3'
services:
  web:
    image: nginx
    ports:
      - "8888:80"
  volumes:
      - ~/mysite:/usr/share/nginx/html
    db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: secret
